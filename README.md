<!-- # Frontend-2---Contest-1---August-24
Frontend 2 || Contest 1 || August 24 -->

<!-- 1. PrintDeveloperbyMap()
Using the .map() function to iterate over the array and filter out the employees with the profession of "developer".
Then, check filtered developers using console.log.

2. PrintDeveloperbyForEach()
Use the .forEach() function to iterate over the array and log the developers.

3. addData()
Add a new employee object to the array and then log the updated array.

4. removeAdmin()
Use .filter() to remove the employee with the profession "admin" and log the updated array.

5. concatenateArray()
Creating a new array with 3 different employee objects and concatenate it with the existing array.
 Log the concatenated array. -->